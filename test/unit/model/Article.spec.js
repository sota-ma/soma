// import Article from '../../../store/model/Article'

describe('article model', () => {
  describe('正常系', () => {
    test('Nullableな値が帰ってくる可能性があるフィールドのデフォルト値のテスト等(もしあれば)', () => {
      // TODO:
      expect(true)
    })
  })
})
