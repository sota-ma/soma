// TODO: getFilteredArticlesは仕様がかなり複雑であるため、テストコードを書く
// import ArticleList from '../../../store/model/ArticleList'

describe('article List model', () => {
  describe('正常系', () => {
    test('日本語タイトルでフィルタリング', () => {
      // TODO:
      expect(true)
    })

    test('英語タイトルでフィルタリング', () => {
      // TODO:
      expect(true)
    })

    test('日本語アブストでフィルタリング', () => {
      expect(true)
    })

    test('英語アブストでフィルタリング', () => {
      expect(true)
    })
  })
})
